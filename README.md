# Production Disabled Placeholder

This branch is intentionally minimal. Use it as the Vercel Production Branch
when the real student web app should not be reachable from the production
Vercel domain.

Deploy preview/staging branches for real testing instead.
